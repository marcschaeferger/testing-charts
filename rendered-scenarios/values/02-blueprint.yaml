# 02-blueprint.yaml - Blueprint enabled with ConfigMap mount

newtInstances:
  - name: "blueprint"
    enabled: true
    auth:
      existingSecretName: "newt-auth"
    blueprintFile: "/etc/newt/blueprint.yaml"
    blueprintData: |
      version: 1
      routes:
        - 10.0.0.0/8
