---
# Source: newt/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: newt-03-docker-socket-newt-sa
  namespace: kube-system
  labels:
    app.kubernetes.io/name: newt
    app.kubernetes.io/instance: newt-03-docker-socket
    app.kubernetes.io/version: "1.10.1"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: newt-1.3.0
automountServiceAccountToken: false
---
# Source: newt/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: newt-03-docker-socket-newt
  namespace: kube-system
  labels:
    app.kubernetes.io/name: newt
    app.kubernetes.io/instance: newt-03-docker-socket
    app.kubernetes.io/version: "1.10.1"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: newt-1.3.0
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get","list","watch"]
---
# Source: newt/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: newt-03-docker-socket-newt
  labels:
    app.kubernetes.io/name: newt
    app.kubernetes.io/instance: newt-03-docker-socket
    app.kubernetes.io/version: "1.10.1"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: newt-1.3.0
subjects:
  - kind: ServiceAccount
    name: newt-03-docker-socket-newt-sa
    namespace: kube-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: newt-03-docker-socket-newt
---
# Source: newt/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: newt-03-docker-socket-newt-main-tunnel
  labels:
    app.kubernetes.io/name: newt
    app.kubernetes.io/instance: newt-03-docker-socket
    app.kubernetes.io/version: "1.10.1"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: newt-1.3.0
    newt.instance: main-tunnel
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: newt-03-docker-socket
    newt.instance: main-tunnel
  ports:
    - name: wg
      port: 51820
      targetPort: 51820
      protocol: UDP
    - name: tester
      port: 51821
      targetPort: 51821
      protocol: UDP
---
# Source: newt/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: newt-03-docker-socket-newt-main-tunnel
  labels:
    app.kubernetes.io/name: newt
    app.kubernetes.io/instance: newt-03-docker-socket
    app.kubernetes.io/version: "1.10.1"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: newt-1.3.0
    app.kubernetes.io/component: newt
    newt.instance: main-tunnel
spec:
  revisionHistoryLimit: 3
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: newt-03-docker-socket
      newt.instance: main-tunnel
  template:
    metadata:
      labels:
        app.kubernetes.io/component: newt
        app.kubernetes.io/instance: newt-03-docker-socket
        newt.instance: main-tunnel
    spec:
      serviceAccountName: newt-03-docker-socket-newt-sa
      automountServiceAccountToken: false
      containers:
        - name: newt
          image: docker.io/fosrl/newt:1.10.1
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 65534
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          env:
            - name: PANGOLIN_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: newt-cred
                  key: PANGOLIN_ENDPOINT
            - name: NEWT_ID
              valueFrom:
                secretKeyRef:
                  name: newt-cred
                  key: NEWT_ID
            - name: NEWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: newt-cred
                  key: NEWT_SECRET
            - name: LOG_LEVEL
              value: "INFO"
            - name: DOCKER_SOCKET
              value: "/var/run/docker.sock"
            - name: DOCKER_ENFORCE_NETWORK_VALIDATION
              value: "true"
          ports:
            - name: wg
              containerPort: 51820
              protocol: UDP
            - name: tester
              containerPort: 51821
              protocol: UDP
          volumeMounts:
            - name: docker-socket
              mountPath: /var/run/docker.sock
              readOnly: true
          resources:
            limits:
              cpu: 200m
              ephemeral-storage: 256Mi
              memory: 256Mi
            requests:
              cpu: 100m
              ephemeral-storage: 128Mi
              memory: 128Mi
      volumes:
        - name: docker-socket
          hostPath:
            path: /var/run/docker.sock
            type: Socket
