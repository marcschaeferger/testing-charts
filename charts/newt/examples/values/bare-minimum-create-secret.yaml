# Minimal values but provide inline credentials (for testing only)
newtInstances:
  - name: main-tunnel
    pangolinEndpoint: "https://pangolin.example.com"
    id: test-id
    secret: test-secret
    enabled: true
