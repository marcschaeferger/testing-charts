# ServiceAccount configuration
newtInstances:
  - name: sa-test
    enabled: true
    auth:
      existingSecretName: newt-auth
serviceAccount:
  create: true
  name: custom-sa
  automountServiceAccountToken: true
  annotations:
    description: "Custom service account for newt"
  labels:
    app: newt
